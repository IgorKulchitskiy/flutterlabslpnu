name: flutterlabslpnu
description: Alarm SMS project
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  connectivity_plus: ^7.0.0
  flutter:
    sdk: flutter
  mqtt_client: ^10.11.9
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  